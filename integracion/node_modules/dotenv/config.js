// This file is used to define the package.json for the project
// It is used to define the name, version, description, main file, scripts, keywords, author, license, and dependencies of the project
// It is also used to define the bin file for the project
// It is used to define the repository for the project
// It is used to define the bugs for the project
// It is used to define the homepage for the project
// It is used to define the engines for the project
// It is used to define the files for the project
(function () {
  require('./lib/main').config(
    Object.assign(
      {},
      require('./lib/env-options'),
      require('./lib/cli-options')(process.argv)
    )
  )
})()
